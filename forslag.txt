boolean seekDirection = true;
int seekSteps = 0;

void spinWhileSearching(){
  if(seekDirection)
    turnRight();
  else
    turnLeft();

  seekSteps--;
  if(seekSteps == 0){
    seekDirection = !seekDirection;
    seekSteps = random(?, ?);
  }

  /* search for enemy */
};